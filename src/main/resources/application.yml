config:
  cors:
    url: ${URL_CORS}

security:
  jwt:
    secretKey: ${SECRET_KEY}
#  auth: # InMemory
#    username: ${USERNAME_AUTH}
#    password: ${PASSWORD_AUTH}

spring:
  profiles:
    active: ${APP_PROFILE:dev}
  jpa:
    open-in-view: false